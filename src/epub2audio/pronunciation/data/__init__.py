"""Bundled data files for the pronunciation subsystem.

Contains ``default_pronunciations.yaml`` — the built-in dictionary of commonly
mispronounced words, loaded via :func:`importlib.resources`.
"""
